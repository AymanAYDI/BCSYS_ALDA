OBJECT Page 50081 Permission Set
{
  OBJECT-PROPERTIES
  {
    Date=21/03/16;
    Time=15:42:26;
    Modified=Yes;
    Version List=NAVW19.00.00.43402;
  }
  PROPERTIES
  {
    CaptionML=[ENU=Permission Sets;
               FRA=Ensembles d'autorisations];
    SourceTable=Table2000000004;
    PageType=List;
    OnAfterGetRecord=BEGIN
                       //PermissionEditable := ISNULLGUID("App ID");
                       RoleIDEditable := FALSE;
                     END;

    OnNewRecord=BEGIN
                  // Allow edits only on a new record - underlying tables do not allow renames.
                  RoleIDEditable := TRUE;
                END;

    OnInsertRecord=VAR
                     PermissionSet@1000 : Record 2000000004;
                   BEGIN
                     PermissionSet.INIT;
                     PermissionSet."Role ID" := "Role ID";
                     PermissionSet.Name := Name;
                     PermissionSet.INSERT;
                     //PermissionEditable := ISNULLGUID("App ID");
                     RoleIDEditable := FALSE;
                     EXIT(FALSE); // Causes UI to stop processing the action - we handled it manually
                   END;

    OnModifyRecord=VAR
                     PermissionSet@1000 : Record 2000000004;
                   BEGIN
                     //IF NOT ISNULLGUID("App ID") THEN
                     //  ERROR(AlterAppPermSetErr);

                     PermissionSet.GET(xRec."Role ID");
                     IF xRec."Role ID" <> "Role ID" THEN BEGIN
                       PermissionSet.RENAME(xRec."Role ID","Role ID");
                       PermissionSet.GET("Role ID");
                     END;
                     PermissionSet.Name := Name;
                     PermissionSet.MODIFY;
                     //PermissionEditable := ISNULLGUID("App ID");
                     EXIT(FALSE); // Causes UI to stop processing the action - we handled it manually
                   END;

    OnDeleteRecord=VAR
                     PermissionSet@1000 : Record 2000000004;
                   BEGIN
                     //IF NOT ISNULLGUID("App ID") THEN
                     //  ERROR(AlterAppPermSetErr);

                     PermissionSet.GET("Role ID");
                     PermissionSet.DELETE;
                     //PermissionEditable := ISNULLGUID("App ID");
                     EXIT(FALSE); // Causes UI to stop processing the action - we handled it manually
                   END;

    ActionList=ACTIONS
    {
      { 10      ;    ;ActionContainer;
                      Name=PermissionActions;
                      CaptionML=[ENU=Permissions;
                                 FRA=Autorisations];
                      ActionContainerType=RelatedInformation }
      { 7       ;1   ;ActionGroup;
                      Name=ShowPermissions;
                      CaptionML=[ENU=Permissions;
                                 FRA=Autorisations];
                      Image=Permission }
      { 6       ;2   ;Action    ;
                      Name=Permissions;
                      ShortCutKey=Shift+Ctrl+p;
                      CaptionML=[ENU=Permissions;
                                 FRA=Autorisations];
                      Promoted=Yes;
                      PromotedIsBig=Yes;
                      Image=Permission;
                      PromotedCategory=Process;
                      Scope=Repeater;
                      OnAction=VAR
                                 Permission@1001 : Record 2000000005;
                                 Permissions@1000 : Page 9803;
                               BEGIN
                                 Permission.SETRANGE("Role ID","Role ID");
                                 Permissions.SETRECORD(Permission);
                                 Permissions.SETTABLEVIEW(Permission);
                                 Permissions.EDITABLE := PermissionEditable;
                                 Permissions.RUN;
                               END;
                                }
    }
  }
  CONTROLS
  {
    { 1   ;0   ;Container ;
                ContainerType=ContentArea }

    { 2   ;1   ;Group     ;
                Name=Group;
                CaptionML=[ENU=Permission Set;
                           FRA=Ensemble d'autorisations];
                GroupType=Repeater }

    { 3   ;2   ;Field     ;
                Name=PermissionSet;
                CaptionML=[ENU=Permission Set;
                           FRA=Ensemble d'autorisations];
                SourceExpr="Role ID";
                Editable=RoleIDEditable }

    { 4   ;2   ;Field     ;
                Name=<Name>;
                CaptionML=[ENU=Name;
                           FRA=Nom];
                SourceExpr=Name }

  }
  CODE
  {
    VAR
      AlterAppPermSetErr@1001 : TextConst 'ENU=You cannot modify application-level permission sets.;FRA=Vous ne pouvez pas modifier les ensembles d''autorisations de niveau application.';
      PermissionEditable@1000 : Boolean;
      RoleIDEditable@1004 : Boolean;

    BEGIN
    END.
  }
}

